import { NumToHhmiPipe } from './num-to-hhmi.pipe';

describe('NumToHhmiPipe', () => {
  it('create an instance', () => {
    const pipe = new NumToHhmiPipe();
    expect(pipe).toBeTruthy();
  });
});
